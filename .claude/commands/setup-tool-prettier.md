# setup-tool-prettier command

## Workflow

1. Verify the repository is intended to use Prettier
2. Ensure `prettier` configuration exists
3. Ensure repository scripts expose formatting consistently
4. Keep format scope aligned with tracked source and config files

## Constraints

- Do not embed language-irrelevant file globs without repository need
