# UI Policy Seeds

- Treat the files in this directory as template-owned seed data for repository-local UI policy.
- Keep layout, design, and interaction policy separate so setup can reconcile each concern explicitly.
- Let selector tooling export resolved policy data without making the selector itself the only source of truth.
- Let repositories keep the full UI catalog, the selector tool, or only the resolved policy according to setup decisions.
